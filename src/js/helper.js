const buttons = document.querySelectorAll(".btn");
const result = document.getElementById("show-display");
const equal = document.getElementById("equal");
const clearDisplay = document.getElementById("input-ac");
const del = document.getElementById("input-de");

export { buttons, result, equal, clearDisplay, del };
